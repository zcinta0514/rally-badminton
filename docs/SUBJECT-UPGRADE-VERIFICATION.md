# 主体视觉第一批本地验收

日期：2026-09-10。分支：`codex/subject-upgrade`。

## 基线与范围

- 初始基线先继承主工作目录的统计功能及配置更新，最终对齐远端 `main` 的 `b5d49f9`。本地原提交 `248667b` 与远端提交的完整源码树均为 `dd39d16a7537972cdf0695b829800d692249ee6e`，仅历史编号不同。
- 改动为人物模型与加载适配、球拍、羽球和球网视觉。规则、球路、联机消息、统计逻辑和赛后演出控制器没有修改。
- 免费素材、许可及重新生成方法见 [CHARACTER-ASSETS.md](CHARACTER-ASSETS.md)。本次没有付费资产、付费插件、外部生成服务或素材上传。

## 验证证据

- 基线 459 项测试通过；最终 `npm test` 为 **482/482 通过**。`npm run build` 成功，66 个本地资源。
- 实际 GLB 为 297572 字节、8931 个三角形、18 个语义骨骼、5 个材质组，无纹理和外部动画。SHA256：`2bd358cca2e5a963c4de5da6d80e576920d15d33b19143b94e509608dc8b242f`。
- 两侧角色在已有位移、朝向与动作时安装模型，17 个原语义关节和拍面中心世界坐标保持不变。跑动、杀球、高远、低位跨步和双方赛后姿态共 74 个采样检查有限顶点、法线及合理体积。
- 暂停中安装立即采用当前姿态；加载失败保留基础角色并允许重试；销毁后的迟到加载释放自身资源；两名角色的颜色、骨骼和材质互不污染。
- 离线模型、Three addons、许可证进入完整资源清单；测试覆盖根路径与项目子路径、资源变化散列、真实 HTTP MIME 以及离线缓存读取。
- 球网旧测试的静止 `z=0` 假设已更新为与初始交织几何完整比较。振动用相对初始形状的位移判定；暂停与新发球复位检查仍有效。

## 浏览器检查

- 服务从本工作目录启动，源码变更后重启快照服务，并重新加载浏览器核对实际模型。
- 球员预览检查正面、侧前方、侧面、准备与杀球姿态，以及红/绿配色切换。修复衣摆间隙、袖根权重、肩颈残面和预览自阴影噪点。
- 正式球场两名角色加载成功；实际操作高远按钮完成发球，随后观察得分及下一分复位。
- 检查默认桌面视口、844×390 和 932×430 两种横屏布局；尺寸切换后等待布局稳定。临时视口覆盖已恢复。
- 未出现新的浏览器错误；Three 对既有 `PCFSoftShadowMap` 有弃用提示。没有为本批扩大渲染升级范围。
- 最终本地资源版本为 `862f29e8e86c3f8f`。旧页面明确出现“新版已备好”后关闭，再打开预览；浏览器经当前 Service Worker 读取的 GLB SHA256 与上文一致，`athlete-import.js` SHA256 为 `153b836a47d1395cba7f05cd0f516b791725162445286b77adb0671ae7188d03`，与磁盘和 HTTP 快照逐字节相同。

## 尚未覆盖的范围

- iPhone 13–17 Safari **尚未真机验收**。上述尺寸检查不代表真机帧率、温升、内存或触控表现。
- 本轮为可运行的首版人物外形；不宣称已经完成人工动画精修。自定义身高、体重、捏脸界面后置；改变肢体长度仍需动作和服装适配。
- 本轮只提供本地预览；未推送这批源码、未发布线上版本。
