# 项目协作规则

保持客观，直接指出逻辑漏洞或错误，给出可验证、可执行的结果，拒绝讨好与空谈。

## 所有修改必须基于最新版本

这是用户明确要求的持续规则，适用于后续每次改动、修复和更新。

1. 修改前先核对 `git status`、当前分支、远端主分支、发布标签和 `git worktree list`。检查相关工作目录和任务是否已有更新或未提交功能。当前目录和本地 `main` 不能自动视为最新版本。
2. 以用户确认的最新版本、提交继承关系和实际功能确定基线；能连接远端时核对远端进展，不能连接时明确验证范围。不能仅凭页面版本号、目录位置或提交日期判断。
3. 当前目录落后时，先完整保留已有改动，再将最新功能和本次修改正确整合。不得用旧工作目录覆盖新版本，也不得只修改版本号来掩盖功能缺失。
4. 整合后核对最新版本的关键功能仍然存在，并运行相关测试和构建。处理合并冲突时同时保留最新功能与用户已有改动。
5. 打开预览或提供测试地址前，确认服务实际使用的目录、源码版本与关键功能。此项目的 Node 服务在启动时构建资源快照；源码更新后必须重启服务，并核验浏览器加载的版本与功能。Service Worker 不会跳过等待，旧标签可能继续使用旧离线缓存；服务更新成功或单次刷新都不能代替页面核验。
6. 分清本地预览、已推送源码和线上部署状态。只有核验实际服务内容后，才能向用户声明相应版本已更新。
